import ComingSoon from '@/components/features/navigation/ComingSoon'

// Temporary: /thoughts is gated by `UNBUILT_ROUTES`. Remove alongside the
// `isUnbuilt` guard in page.tsx when the page ships.
export default ComingSoon
