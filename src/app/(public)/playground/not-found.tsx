import ComingSoon from '@/components/features/navigation/ComingSoon'

// Temporary: /playground (and its /playground/[case] subpages) is gated by
// `UNBUILT_ROUTES`. Remove alongside the `isUnbuilt` guards in page.tsx when
// the section ships.
export default ComingSoon
